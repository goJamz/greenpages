# Allowlist: send only what the Dockerfile copies into the build context.
# Everything else (real-data CSVs, loader SQL, .env files, notes) stays on the host.
# If the Dockerfile starts copying a new path, re-include it here.
*
!ecma.pem
!go.mod
!go.sum
!cmd/
!cmd/**
!internal/
!internal/**
!scripts/
!scripts/**
