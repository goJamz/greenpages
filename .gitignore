# GitHub working copy only; project-level .gitignore files mirror GitLab.

# Local-only context file.
/notes.md

# Credentials.
.env
.env.*

# Real-data CSV exports must never reach commercial GitHub.
greenpages_*.csv

# Enterprise CA certificate stays on the IL5 side.
app/backend/backend/ecma.pem
